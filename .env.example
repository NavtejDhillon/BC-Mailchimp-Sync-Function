# =============================================================================
# Mailchimp Configuration
# =============================================================================
# Get your API key from: Mailchimp Account > Extras > API Keys
MAILCHIMP_API_KEY=

# Server prefix from your API key (e.g., us1, us2, us3, eu1)
# Found in your API key URL: https://usX.api.mailchimp.com
MAILCHIMP_SERVER_PREFIX=

# Audience/List ID where contacts will be synced
# Get from: Mailchimp Audience > Settings > Audience name and defaults
MAILCHIMP_LIST_ID=

# =============================================================================
# Business Central Configuration
# =============================================================================
# Azure AD Tenant ID where Business Central is hosted
# Get from: Azure Portal > Azure Active Directory > Overview > Tenant ID
BC_TENANT_ID=

# Business Central environment name (e.g., Production, Sandbox)
BC_ENVIRONMENT=Production

# Company ID (GUID) - Required for all BC API operations
# Get using: Get-Company-Id.ps1 or from Business Central
BC_COMPANY_ID=

# =============================================================================
# Authentication Configuration
# =============================================================================
# Set to 'true' to use Managed Identity (recommended for Azure deployments)
# Set to 'false' to use Service Principal authentication (local development)
USE_MANAGED_IDENTITY=false

# Azure AD App Registration credentials (required if USE_MANAGED_IDENTITY=false)
# Get from: Azure Portal > Azure Active Directory > App registrations > Your App
BC_CLIENT_ID=
BC_CLIENT_SECRET=
